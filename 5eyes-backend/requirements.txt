fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]>=3.2,<4
# Roadmap #90: versionierte Schema-Migrationen fuer Postgres-Produktion
# (SQLite-Dev/Test-Pfad bleibt unveraendert bei create_all).
alembic==1.19.0
pydantic==2.9.2
pydantic-settings==2.5.2
# Sprint A merge: develop fuehrt EmailStr ein, braucht email-validator.
email-validator>=2.0,<3
# JWT: PyJWT statt python-jose (2026-07-18). python-jose 3.3.0 ist unmaintained
# + CVE-behaftet (Algorithm-Confusion). Nur HS256, kein JWE -> Wechsel neutral.
PyJWT[crypto]==2.10.1
# Tenant-DEK envelope encryption (Postgres/Tier-2 isolation hardening).
cryptography>=42,<45
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
aiofiles==24.1.0
apscheduler==3.10.4
yfinance==0.2.54
# numpy/scipy: EXAKT gepinnt seit 2026-08-19 (vorher offene Ranges
# >=1.26,<3 / >=1.11,<2). Grund: der Golden-Snapshot-Regressionstest
# (test_golden_snapshot_ch_regression.py) verglich exakte CHF-Betraege
# einer stochastischen Optimierung (SLSQP) und driftete zwischen einem
# lokalen Dev-Environment (numpy 2.4.4/scipy 1.17.1) und einem frischen
# CI-Install (numpy 2.5.2/scipy 1.18.0) um ein paar hundert CHF pro
# Rebalancing-Schritt -- SLSQP-Konvergenz ist minimal versionsabhaengig.
# Nicht nur ein CI-Problem: ein Wealth-Management-Engine darf nicht je
# nach zufaellig installierter numpy/scipy-Patch-Version unterschiedliche
# Empfehlungen liefern. Exaktes Pinning macht das Ergebnis reproduzierbar
# unabhaengig vom Install-Zeitpunkt. Bei jedem bewussten Upgrade: neu
# pinnen UND die Golden-Snapshot-Fixtures unter der neuen Version neu
# einfrieren (tests/fixtures/golden_ch_recommendations/*.json).
numpy==2.5.2
scipy==1.18.0
# reportlab: pure-Python PDF-Engine fuer Kundendokumente (Anlagestrategie,
# Risikoprofil, Quartalsbericht). PyInstaller-tauglich, kein Native-Binary.
reportlab>=4.0,<5
# pypdf: test-only PDF text/page assertions for generated reports.
pypdf>=4,<6
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
pyyaml==6.0.2
# segno: 2FA-QR-Code (pure-python, keine C-Abhaengigkeit). Fehlt sie, faellt
# das 2FA-Setup graceful auf das Text-Secret zurueck (kein 500).
segno==1.6.6
# Optional for encrypted production builds:
# sqlcipher3-binary==0.5.4
